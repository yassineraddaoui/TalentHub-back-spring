spring:
    datasource:
        username: user
        password: password
        url: jdbc:mysql://mysql-container:3306/offre_emploi?createDatabaseIfNotExist=true&useSSL=true
    jpa:
        hibernate:
            ddl-auto: update
    main:
        allow-circular-references: true
    servlet:
        multipart:
            max-file-size: 100MB
            max-request-size: 100MB
    mail:
        host: smtp.gmail.com
        port: 587
        username: gimmework3@gmail.com
        password: qibmqihwcagjdihi
        properties:
            mail:
                smtp:
                    auth: true
                    starttls:
                        enable: true

server:
    port: 8080

management:
    endpoints:
        web:
            exposure:
                include: "*"
